import React from "react";

export default function filterBar() {
  return (
    <div id="filter-bar">
      <button>All</button>
      <button>Completed</button>
      <button>In Progresss</button>
    </div>
  );
}
